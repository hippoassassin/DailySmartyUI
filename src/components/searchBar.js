import React, { Component } from "react";

class Logo extends Component {
  render() {
    return (
      <form className="logo-main">
        <img />
      </form>
    );
  }
}

export default Logo;
